**Update:**

Vendor session completed. During the session, the following was demonstrated:

* Direct `curl` to the Entra token endpoint → **connection timeout**.
* Same endpoint through corporate proxy → **successful connection**.
* UCD `net.properties` shows `java.net.useSystemProxies=false` and no proxy configured.
* UCD OIDC Discovery using the Entra endpoint also → **timeout**.
* UCD logs continue to show `java.net.ConnectException: Connection timed out` during token exchange.

Vendor advised involving Network. Network has confirmed no traffic is visible for the failed direct connection.

**Next step:** Engage OS specialist to validate the server/JVM proxy configuration and determine how UCD should be configured to use the working corporate proxy path.

**Status:** OIDC issue remains under investigation; no configuration changes made.
