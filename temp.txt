The Bitbucket Production environment is currently configured to use the LDAP user directory over **TCP/389 with SSL/TLS disabled**. As the DR environment is a clone of the Production environment, we are requesting the same firewall access to maintain configuration consistency and ensure the DR instance functions identically to Production.

Please find the attached Production Bitbucket user directory configuration showing the LDAP settings (TCP/389, SSL disabled).
