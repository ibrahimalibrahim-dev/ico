Migration of PR Validation Environment to Secure Works

Hi Team,

Following the discussion and management approval, we will proceed with migrating the PR validation activities to the Secure Works environment due to the ongoing CBS performance and testing activities on the current PR server.

As this is a new deployment environment, a number of prerequisite configurations will be required before application validation can resume. As an initial step, could you please confirm the following:

1. The Java version installed on the Secure Works server is compatible with the application requirements.
2. The configured `JAVA_HOME` is set appropriately for the installed Java version.
3. The application deployment directory is available and confirmed (e.g. `<APPLICATION_DEPLOYMENT_PATH>`).
4. The shared library/upload directory is available and confirmed (e.g. `<SHARED_LIBRARY_PATH>`).

Once the above has been confirmed, we will proceed with the remaining environment-specific configuration activities (e.g. UCD agent installation, connectivity, firewall validation, and deployment verification) before continuing PR validation.

This will also provide traceability of the environment baseline for subsequent testing activities.

Kindly let us know once the above prerequisites have been completed so that we can proceed with the next phase.

Regards,

[Your Name]



Following the discussion and management approval, we will proceed with migrating the PR validation activities to the Secure Works environment...

Before proceeding, could you please confirm the intended purpose/classification of the Secure Works environment (e.g. PR/Test, Pre-Production, or Production) so that the deployment and validation activities can be aligned accordingly.

As this is a new deployment environment, could you also please confirm:

Java version...
JAVA_HOME...
...
