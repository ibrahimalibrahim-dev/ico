**Security/ISD dependencies have introduced additional pre-RET requirements:**

* ISD requested a dedicated service account, as the existing `s_devOps` account cannot be reused across tools. The account creation has been approved and completed; BeyondTrust onboarding is still pending.
* As the server was commissioned from the approved Red Hat golden image, ISD requested written confirmation regarding the OS checklist, as this is not normally completed separately for golden-image-based OS commissioning.
* OS has confirmed that the service account cannot be onboarded to the server while it remains in staging. The server will only be promoted to production following RET approval, CAB approval, and execution of the promotion activity.

**Current status:** Service account creation is complete. Remaining items are BeyondTrust onboarding and resolution/confirmation of the staging-to-production onboarding dependency before RET can proceed.
