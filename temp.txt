Following our assessment of the production Bitbucket server storage, the application volume (`/u01`) is currently allocated with **1 TB**, of which approximately **394 GB** is utilized.

The Bitbucket Home directory accounts for the majority of the storage usage, with the following breakdown:

* `Atlassian/home/stash` (active Bitbucket data): **240 GB**
* `Atlassian/home/stash - backup 2020` (historical backup): **157 GB**

After reviewing the contents with the application team, the `stash - backup 2020` directory has been identified as a historical backup and is **not required for the initial DR clone**. If needed, it can be archived and transferred separately at a later stage.

Accordingly, please proceed with cloning the production server while excluding the `Atlassian/home/stash - backup 2020` directory. The active Bitbucket application data under `Atlassian/home/stash` should be included in the clone.

Please proceed with the cloning activity at the earliest convenience.
