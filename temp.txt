Just to clarify, the current error is occurring during Jenkins multibranch SCM checkout / PR merge validation, before the pipeline or Jenkinsfile starts.

So this would not be a normal pipeline-layer change. To apply something like --allow-unrelated-histories, it would likely require changes to Jenkins SCM/plugin configuration rather than the application pipeline itself.

Given that, it would be better to first validate the repository branch ancestry / target branch setup, since the current error indicates unrelated histories between the PR branches.
