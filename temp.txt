Hi Team,

Thank you for the update and for sharing the Bitbucket merge details.

We have reviewed the current Jenkins behavior further. As this repository uses a Multibranch Pipeline, the reported failure is occurring during the SCM checkout / PR merge validation step, prior to execution of any pipeline stages or Jenkinsfile logic.

Current error:

`checkout failed: hudson.plugins.git.GitException`
`git merge ... fatal: refusing to merge unrelated histories`

This indicates the failure is happening at Git branch ancestry / merge validation level during PR discovery, rather than within the pipeline itself.

We also reviewed the current branch graph/history and the present `master` branch appears isolated from the primary commit lineage, while the main historical timeline appears associated with `development` / previous master history. This would be consistent with the unrelated histories error being returned during PR validation.

Given the above, could the repository branch lineage and intended PR target branch please be revalidated from the Bitbucket side.

Once branch ancestry is aligned, we are happy to continue support from the Jenkins side if any further issue remains.

Regards,
[Your Name / Team]
